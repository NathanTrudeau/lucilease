services:
  lucilease:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    container_name: lucilease
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      POLL_SECONDS: "300"
    volumes:
      - ./data:/data
      - ./fixtures:/fixtures:ro
